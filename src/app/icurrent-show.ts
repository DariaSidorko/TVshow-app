export interface ICurrentShow {
  name: string,
  language: string,
  genres: string,
  officialSite: string,
  runtime: number,
  time: string,
  days: string,
  rating: number,
  network: string, //network -> name
  country: string, //country->code
  image: string,
  summary: string,
}

