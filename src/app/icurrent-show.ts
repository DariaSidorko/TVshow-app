export interface ICurrentShow {
  name: string,
  language: string,
  genres: string,
  officialSite: string,
  runtime: number,
  showTime: string,
  raiting: number,
  network: string, //network -> name
  country: string, //country->code
  image: string,
  summary: string,
}

